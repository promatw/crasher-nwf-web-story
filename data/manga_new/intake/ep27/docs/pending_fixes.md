# JP27 pending_fixes.md

Current package state: no blocking pending_fix for manga selection.

Non-blocking notes:

1. Canvas sizes are mixed because HM re-uploaded 001–003 selected at 864 × 1536, while 004–015 are 941 × 1672. This package preserves images as provided and does not normalize.
2. Text / dialogue is draft only and requires HM / Nana2 / PM2 finalization.
3. If UXE1 requires strict 864 × 1536, perform a separate full-package normalization step.
